<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Laravel</title>
    <style>
        body {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            margin: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: radial-gradient(circle at top left, #4f46e5, #111827);
            color: #f9fafb;
        }
        .card {
            background: rgba(15,23,42,0.9);
            border-radius: 1.5rem;
            padding: 2.5rem 3rem;
            box-shadow: 0 24px 80px rgba(15,23,42,0.8);
            text-align: center;
            max-width: 520px;
        }
        h1 {
            font-size: 2.5rem;
            margin-bottom: 0.75rem;
        }
        p {
            margin: 0.25rem 0;
            color: #e5e7eb;
        }
        code {
            background: rgba(31,41,55,0.9);
            padding: 0.25rem 0.5rem;
            border-radius: 0.5rem;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
<div class="card">
    <h1>Laravel is running</h1>
    <p>Your new application has been scaffolded.</p>
    <p>Run <code>composer install</code> and then <code>php artisan serve</code> to get started.</p>
</div>
</body>
</html>

